spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    com.bank.loan: DEBUG
    org.hibernate.SQL: DEBUG

management:
  endpoint:
    health:
      show-details: always

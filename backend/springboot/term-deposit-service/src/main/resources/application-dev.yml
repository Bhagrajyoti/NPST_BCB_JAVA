spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

logging:
  level:
    com.bank.termdeposit: DEBUG
    org.hibernate.SQL: DEBUG

management:
  endpoint:
    health:
      show-details: always
